apiVersion: v1
kind: Service
metadata:
  name: spring-boot-web-service
spec:
  selector:
    app: spring-boot-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
